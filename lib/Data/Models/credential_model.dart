// lib/data/models/credential_model.dart (optional, not used now)
import '../../Domain/Entities/credential_entity.dart';

class CredentialModel extends CredentialEntity {
  CredentialModel(String url, String password) : super(url, password);
}